#macro RES_W 1024
#macro RES_H 768

display_set_gui_size(RES_W,RES_H);

global.hasPMG = false;
global.hasRL = false;

global.kills = 0;
global.killsthisroom = 0;
killstextscale = 1;

display_reset(0,false);